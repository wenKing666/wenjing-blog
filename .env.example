# ---------------------------------------------------------------------------
# 本地开发用。生产环境请把同样的内容写到 /opt/myblog/shared/blog.env，
# 那个文件在构建产物之外，重新部署不会覆盖它。
#
# 复制成 .env.local 后填写；.env.local 已被 .gitignore 排除。
# ---------------------------------------------------------------------------

# ⚠️ 下面两个值里**都不能出现 $**。
# Next 加载 .env 时会做一遍变量展开，`$ABC` 会被当成变量名替换掉，
# 值会被悄悄截断 —— 表现为"密码明明对却一直提示密码不正确"。很难查。
# （本项目的摘要格式已改用 `.` 分隔，正常不会踩到；但你自己改值时请留意。）

# 【必填】管理密码的 scrypt 摘要。用 `npm run set-password` 生成。
ADMIN_PASSWORD_HASH=

# 【必填】会话 cookie 的签名密钥，至少 32 字符。用 `openssl rand -base64 48` 生成。
# 换掉它 = 所有已登录的会话立即失效。
SESSION_SECRET=

# 【生产必填】内容目录的绝对路径。
# 不填的话生产环境会**直接报错拒绝启动** —— 这是有意的：
# 回落到 process.cwd() 会把文章写进构建产物里，下次部署就没了。
# 开发环境不需要设，默认用项目根下的 content/
# CONTENT_DIR=/opt/myblog/content

# cookie 的 secure 属性。
# 走 HTTPS 时设 true；**纯 HTTP 访问必须设 false**，
# 否则浏览器不会回传 cookie，表现为"密码明明对了却一直跳回登录页"。
# 不设的话会根据 SITE_URL 是否为 https:// 自动判断。
# COOKIE_SECURE=false

# 站点对外地址，用于自动判断 cookie 的 secure 属性。
# SITE_URL=https://example.com

# 本地开发时想换个端口就设这个（注意：生产环境 PORT 必须写在 systemd 里，
# 不能放在 .env —— Next 的 server.js 在任何 .env 加载之前就读了它）
# PORT=3000
